// Arrow functions are used for anonymous functions. These are function that do not have a name.
// When creating an anonymous function you will have your parameter list (name, message) then the => {<do something here>}
// You can also export your anonymous functions as well. As the default.

export default (userName) => {
    return "Hello " + userName;
}