/*
    Your Task is to write a new function that is named "combine" and have the following characteristics:
        * Accept three input values
        * Calculate a new value based on the three input values "a * b / c"
        * Return the calculated result
 */

function combine(firstNumber, secondNumber, thirdNumber) {
    return firstNumber * secondNumber / thirdNumber;
}
